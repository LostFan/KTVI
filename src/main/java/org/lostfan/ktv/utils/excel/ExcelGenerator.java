package org.lostfan.ktv.utils.excel;

public interface ExcelGenerator {

    /**
     * Generates and opens an Excel file.
     * @return message code as a result of the generation.
     */
    String generate();
}
